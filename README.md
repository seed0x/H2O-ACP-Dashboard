# Plumbing Ops Platform (MVP v1)

Monorepo for a multi-tenant plumbing ops platform. Backend: FastAPI + Postgres. Frontend: Next.js + TypeScript.

Quickstart:

```bash
cp .env.example .env
make dev
```

Then:
- Run migrations: `make migrate`
- Seed: `make seed`

See docs for more.
